<!-- Mobile Bottom Navigation Bar - Apple Glass Style -->
<nav class="mobile-bottom-nav {% if template.name != 'index' %}is-visible{% endif %}" id="mobileBottomNav" aria-label="Mobile navigation">
  <a href="/" class="mobile-bottom-nav__item {% if template.name == 'index' %}mobile-bottom-nav__item--active{% endif %}" aria-label="Home">
    <svg class="mobile-bottom-nav__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
      <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
      <polyline points="9 22 9 12 15 12 15 22"></polyline>
    </svg>
  </a>

  <a href="#" id="openKoraaSearchMenu" class="mobile-bottom-nav__item" aria-label="Search">
    <svg class="mobile-bottom-nav__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
      <circle cx="11" cy="11" r="8"></circle>
      <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
    </svg>
  </a>

  <a href="#" id="openHmFavouritesTrigger" class="mobile-bottom-nav__item" aria-label="Favourites">
    <div class="mobile-bottom-nav__cart-wrapper">
      <svg class="mobile-bottom-nav__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
        <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
      </svg>
      <span class="mobile-bottom-nav__badge" id="wishlistBadge" style="display:none;">0</span>
    </div>
  </a>

  <a href="#" class="mobile-bottom-nav__item {% if template.name == 'customers/account' or template.name == 'customers/login' %}mobile-bottom-nav__item--active{% endif %}" aria-label="Account" onclick="event.preventDefault();if(typeof openAccountDrawer==='function')openAccountDrawer();">
    <svg class="mobile-bottom-nav__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
      <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
      <circle cx="12" cy="7" r="4"></circle>
    </svg>
  </a>

  <a href="/cart" class="mobile-bottom-nav__item {% if template.name == 'cart' %}mobile-bottom-nav__item--active{% endif %}" aria-label="Cart">
    <div class="mobile-bottom-nav__cart-wrapper">
      <svg class="mobile-bottom-nav__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
        <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path>
        <line x1="3" y1="6" x2="21" y2="6"></line>
        <path d="M16 10a4 4 0 0 1-8 0"></path>
      </svg>
      <span class="mobile-bottom-nav__badge" id="cartBadge" style="{% if cart.item_count == 0 %}display:none;{% else %}display:flex;{% endif %}">{{ cart.item_count }}</span>
    </div>
  </a>
</nav>

<script>
  // Update cart badge count
  (function() {
    window.updateCartBadge = function() {
      fetch('/cart.js?t=' + Date.now(), { cache: 'no-store' })
        .then(r => r.json())
        .then(cart => {
          const badge = document.getElementById('cartBadge');
          if (badge && cart.item_count > 0) {
            badge.textContent = cart.item_count;
            badge.style.display = 'flex';
          } else if (badge) {
            badge.style.display = 'none';
          }
        }).catch(() => {});
    }
    
    // Shopify dynamic cart sync hooks
    document.addEventListener('cart:update', window.updateCartBadge);
    document.addEventListener('cart:updated', window.updateCartBadge);
    document.addEventListener('cart:change', window.updateCartBadge);
    document.addEventListener('cart:refresh', window.updateCartBadge);

    // Global Wishlist Badge Update
    window.updateWishlistBadge = function() {
      try {
        const list = JSON.parse(localStorage.getItem('greyexim_wishlist')) || [];
        const count = list.length;
        const bottomBadge = document.getElementById('wishlistBadge');
        if (bottomBadge) {
          bottomBadge.textContent = count;
          bottomBadge.style.display = count > 0 ? 'flex' : 'none';
        }
        const headerBadge = document.getElementById('wishlist-header-badge');
        if (headerBadge) {
          headerBadge.textContent = count;
          headerBadge.style.display = count > 0 ? 'flex' : 'none';
        }
      } catch(e) {}
    };
    
    if (document.readyState === 'complete') window.updateWishlistBadge();
    else window.addEventListener('load', window.updateWishlistBadge);
  })();  // Scroll-based reveal for bottom nav (ONLY on homepage)
  {% if template.name == 'index' %}
  (function() {
    var nav = document.getElementById('mobileBottomNav');
    if (!nav) return;
    var scrollThreshold = 100; // px to scroll before showing
    var ticking = false;

    function onScroll() {
      if (!ticking) {
        requestAnimationFrame(function() {
          if (window.scrollY > scrollThreshold) {
            nav.classList.add('is-visible');
          } else {
            nav.classList.remove('is-visible');
          }
          ticking = false;
        });
        ticking = true;
      }
    }

    window.addEventListener('scroll', onScroll, { passive: true });
    // Initial check
    onScroll();
  })();
  {% endif %}
</script>
